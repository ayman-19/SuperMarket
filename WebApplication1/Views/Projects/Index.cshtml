@model IEnumerable<WebApplication1.Models.Project>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plocation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DnumNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DnumNavigation.Dnum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Pnumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Pnumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Pnumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
